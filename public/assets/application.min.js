(function() {
  jQuery(function() {
    var init, issuePath;
    init = (function() {})();
    issuePath = 'https://api.github.com/repos/laravel/framework/issues';
    return $.getJSON(issuePath, function(response) {
      var closed, issue, open, _i, _len;
      open = 0;
      closed = 0;
      for (_i = 0, _len = response.length; _i < _len; _i++) {
        issue = response[_i];
        if (issue.state === "open") {
          open++;
        } else {
          closed++;
        }
        $('body').append("<p><img width='50px' src='" + issue.user.avatar_url + "'/> " + issue.user.login + "</p>");
      }
      $('body').append("<p>closed: " + closed + " open: " + open + "</p>");
      return console.log(open);
    });
  });

}).call(this);
